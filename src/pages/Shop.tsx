import { ProductGrid } from '@/components/ProductGrid';

const Shop = () => {
  return (
    <main>
      <div className="pt-8">
        <ProductGrid />
      </div>
    </main>
  );
};

export default Shop;
